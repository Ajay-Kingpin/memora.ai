git init
git config user.email "you@example.com"
git config user.name "Your Name"
git add .
git commit -m "Initial commit: Layer 1 & 2 implementation"
git remote add origin https://github.com/Ajay-Kingpin/memora.ai.git
git branch -M main
# We won't run push here because it's the one most likely to hang
